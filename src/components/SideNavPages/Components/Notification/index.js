import React from 'react';
import './style.styl';
import { Button, Snackbar, MessageBar } from '../../../';
import TabsTemplate from '../../../TabsTemplate'

function NotificationPage(props){
  return(
    <div>
      <TabsTemplate label="Notification"/>
    </div>
  )

}

export default NotificationPage;
